# API Documentation

`API_Specification_Initial.md` contains the initial API contract for Increment 1.

The backend owner is Member 01. The document should be updated whenever endpoint
paths, DTOs, validation rules or response formats are finalized.
